#include<stdio.h>
#include<string.h>
int main()
{
	char str1[] = "Bro";
	char str2[] = "Code";
	int result ;
	
//	strncat (str2 , str1 ,2);
	//strnset(str1 , 's' , 2);
  // int result = strlen(str1);
//	printf("%s",str1);
//	printf("%d",result);
    /* result = strcmpi(str1, str2  );
     if (result == 0)
     {
     	printf(" str same ");
	 }
	 else 
	 {
	 	printf("str not same ");
	 } */
	return 0 ;
}
