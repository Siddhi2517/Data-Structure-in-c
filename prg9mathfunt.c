//math functions
#include<stdio.h>
#include<math.h>
int main()
{
	double A = sqrt(25);
	double B = pow(4,5);
	int C = round(76.23);
	int D = ceil(10.22);
	int E = floor(43.97);
	double F = fabs(-157);
	double G = fabs(157);
	double H = log(5);
	double I = sin(30); // Took values in radian formate
	double J = cos (60);
	double K = tan (45);
	
	printf("%lf",I);
	return 0;
	
}
